manifest:
  remotes:
    - name: zmkfirmware
      url-base: https://github.com/zmkfirmware
  projects:
    - name: zmk
      remote: zmkfirmware
      # Przypięte do stabilnego taga zamiast ruchomego main:
      # central i peripheral MUSZĄ być budowane z identycznej wersji ZMK,
      # inaczej split BLE nie zestawi szyfrowania (Security failed / timeout 0x08).
      revision: v0.3.0
      import: app/west.yml
  self:
    path: config
