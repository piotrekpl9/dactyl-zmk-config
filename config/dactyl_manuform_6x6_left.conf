# ─────────────────────────────────────────────────────────
# Konfiguracja ZMK – Dactyl Manuform 6x6 – LEWA POŁOWA
# Rola: CENTRAL (podłączona do komputera, obsługuje ZMK Studio)
# (rola CENTRAL przypisana w Kconfig.defconfig dla shielda LEFT)
# ─────────────────────────────────────────────────────────

# Bluetooth
CONFIG_BT=y
CONFIG_ZMK_BLE=y
CONFIG_BT_DEVICE_NAME="Dactyl MF6x6 v2"

# Podzielona klawiatura – lewa połowa jest CENTRAL
CONFIG_ZMK_SPLIT=y
CONFIG_ZMK_SPLIT_BLE=y

# ZMK Studio – konfigurowane przez build.yaml (snippet + cmake-args)
CONFIG_ZMK_STUDIO_LOCKING=n

# Debounce
CONFIG_ZMK_KSCAN_DEBOUNCE_PRESS_MS=5
CONFIG_ZMK_KSCAN_DEBOUNCE_RELEASE_MS=5

# Uśpienie – WYŁĄCZONE na czas diagnozy parowania split (patrz right.conf).
CONFIG_ZMK_SLEEP=n
# CONFIG_ZMK_PM_SOFT_OFF=y



